# 작업 보고서: legacy-red-case-tests

## 개요

- 작업 일시: 2026-05-22 09:41 KST
- 작업 목적: 레거시 코드의 결함으로 발생할 수 있는 RED CASE 테스트를 설계하고, 테스트 케이스 요약 문서를 작성한다.
- 핵심 결과: 구현 코드와 빌드 스크립트를 수정하지 않고 C++17 기반 RED CASE 테스트 코드와 HTML 요약 문서를 추가했다.

## 변경 내용

- 수정/추가한 파일:
  - `tests/red_cases/legacy_red_cases_test.cpp`
  - `docs/legacy_red_case_test_summary.html`
  - `report/04.legacy-red-case-tests_report.md`
  - `prompting/04.legacy-red-case-tests_prompt.md`
- 주요 구현 내용:
  - `RED-04`: 감정 분석과 감정 필터 기준 불일치 테스트
  - `RED-05`: 카테고리 분석과 카테고리 필터 기준 불일치 테스트
  - `RED-14`: 세션 정적 상태 오염 테스트
  - `RED-15-A`: 필터링 중 사용자 입력 원문 stdout 출력 테스트
  - `RED-15-B`: 디버그 로그 기본 활성화 위험 테스트
  - Catch2가 있으면 Catch2로 동작하고, 아직 Catch2가 연결되지 않은 환경에서는 C++17 fallback runner로 문법 검사가 가능하도록 구성했다.
- 테스트 변경 내용:
  - RED CASE 테스트 소스 파일을 추가했다.
  - 테스트 타깃과 빌드 스크립트는 수정하지 않았다.

## 검증

- 실행한 빌드 명령: 실행하지 않음
- 실행한 테스트 명령:
  - `g++ -std=c++17 -I src/cpp -fsyntax-only "tests/red_cases/legacy_red_cases_test.cpp"`
- 테스트 결과:
  - C++17 문법 검사 통과
  - 실제 테스트 실행은 프로젝트 테스트 타깃이 아직 없어 수행하지 않음
- 커버리지 확인 결과: 해당 없음
- 문서 및 코드 진단:
  - `tests/red_cases/legacy_red_cases_test.cpp`: linter 오류 없음
  - `docs/legacy_red_case_test_summary.html`: linter 오류 없음

## GitHub 업로드

- 브랜치: `RED`
- 커밋 메시지: `Add legacy RED case tests`
- 커밋 해시: `209e5a4`
- 원격 저장소: `origin` (`https://github.com/jskor013/feedback_analyzer_13.git`)
- Push 결과: `RED` 브랜치를 `origin/RED`에 업로드 완료

## 주의 사항

- 남은 리스크:
  - 현재 프로젝트에 Catch2 테스트 타깃이 없어 실제 테스트 실행은 후속 작업이 필요하다.
  - `RED-01`, `RED-02`, `RED-06`~`RED-10`, `RED-13`은 현재 구조상 `main.cpp` 내부 static 함수 또는 HTTP 실행 흐름에 묶여 있어 후속 구조 분리 후 자동화가 필요하다.
  - 기존 미추적 파일 `docs/requirement.md`, `Cursoe AI 퀴즈 - 제출.md`는 이번 커밋 대상에서 제외한다.
- 후속 작업:
  - Catch2 테스트 타깃을 별도 커밋에서 추가한다.
  - CSV 처리와 서버 시작 흐름이 분리된 뒤 나머지 RED CASE를 자동화한다.
