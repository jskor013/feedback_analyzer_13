# 프롬프트 기록: legacy-analysis-planning

## Export 원칙

- 이 파일은 사용자 Prompt와 AI 답변의 Export 기록이다.
- 요약본보다 원문 보존을 우선하지만, 도구 출력과 일부 긴 문서 본문은 시스템 한계로 주요 내용과 생략 사유를 표시한다.
- 숨겨진 시스템 메시지와 비공개 내부 추론은 제외한다.

## 대화 전문

### Turn 1 - User Prompt

```text
당신은 C++ 코드 분석 전문가입니다.
워크스페이스: e:\DEV\feedback_analyzer_13
⚠️ 코드 작성·클래스 설계·알고리즘·빌드 스크립트는 하지 마십시오.

Observation
1) Regacy Code에 현재 존재하는 문제점은 무었인가?
2) 전체 구조에 존재하는 문제점은 무엇이며 어떤 위험이 있는가?

출력: 구조화된 Markdown만. 코드·파일 경로 금지.
```

### Turn 1 - AI Answer

```text
## 종합 판단

현재 코드는 작은 데모 수준에서는 동작할 수 있으나, 실제 사용자·동시 요청·데이터 품질·기능 확장 상황에서는 오류와 상태 오염 위험이 큽니다. 핵심 원인은 전역 상태, 단일 지점 책임 집중, 중복된 판단 기준, 입력 데이터 처리의 취약성입니다.

## 1. Legacy Code에 현재 존재하는 문제점

기능 정확성 문제, 상태 관리 문제, 입력·예외 처리 문제, 유지보수성 문제를 구조화해 보고했습니다.

## 2. 전체 구조의 문제점과 위험

단일 중심 구조, 모듈 경계 불명확, 동시성 위험, 데이터 신뢰성 위험, 테스트 가능성 부족, 운영 위험을 보고했습니다.

## 결론

가장 큰 문제는 개별 버그보다 구조적 결합과 공유 상태입니다. 현재 구조에서는 입력, 분석, 필터, 다운로드가 서로 강하게 묶여 있어 기능이 늘어날수록 오류가 누적될 가능성이 높습니다.
```

### Turn 2 - User Prompt

```text
당신은 C++ 코드 분석 전문가입니다.
워크스페이스: e:\DEV\feedback_analyzer_13
⚠️ 코드 작성·클래스 설계·알고리즘·빌드 스크립트는 하지 마십시오.

Observation
3) 추가적인 코드에서 발견되는 버그 위험이 있는지 분석해줘.

출력: 구조화된 Markdown만. 코드·파일 경로 금지.
```

### Turn 2 - AI Answer

```text
## Observation 3. 추가 코드에서 발견되는 버그 위험

다운로드 결과 오염 위험, 분석 결과와 필터 결과 불일치 위험, CSV 처리 오류 위험, 동시 요청 처리 위험, 입력값 검증 부족 위험, HTML 출력 안정성 위험, 키워드 매칭 오분류 위험, 카테고리 필터 누락 위험, 초기화 순서 의존 위험, 사용되지 않는 구성 요소로 인한 혼동 위험을 보고했습니다.

## 핵심 위험 요약

가장 중요한 추가 버그 위험은 사용자 간 데이터가 섞일 수 있는 공유 상태 문제, 분석·필터·다운로드 결과가 서로 일치하지 않을 수 있는 기준 불일치 문제, CSV와 입력값 처리의 취약성으로 인한 데이터 유실·오분류 문제입니다.
```

### Turn 3 - User Prompt

```text
너는 코드 분석 전문가야.
위의 코드 문제점의 분석 결과를 요약한 Report를 작성해줘.
docs 폴더에 Regacy_Code_분석_리포트.md
로작성해줘.
개발자가 이 문서를 읽고 수정방향을 잘 이해할 수 있도록 작성해줘.
```

### Turn 3 - AI Answer

```text
작성 완료했습니다.

`docs/Regacy_Code_분석_리포트.md`에 코드 문제점 분석 리포트를 추가했습니다. 개발자가 수정 방향을 이해할 수 있도록 문제 요약, 레거시 코드 이슈, 추가 버그 위험, 구조적 위험, 수정 우선순위, 권장 개선 방향 순서로 정리했습니다.

문서 진단도 확인했고, 현재 linter 오류는 없습니다.
```

### Turn 4 - User Prompt

```text
@docs/Regacy_Code_분석_리포트.md 
[p]너는 리팩토링 플랜을 짜는 C++ 개발 리더야.
[T] 분석 리포트를 보고 리팩토링을 위한 TO-DO_List를 짜줘.
  - 중요성을감안한 Refactoring 순서
  - 리스크 관리를 위해 1커밋 단위를 설정하여 계획 작성
[F] README.md 파일로 작성
```

### Turn 4 - AI Answer

```text
`README.md`로 리팩토링 TODO List를 작성했습니다.

내용은 분석 리포트를 바탕으로 중요도 순서, 1커밋 단위 계획, 각 커밋별 TODO와 검증 항목, 권장 커밋 메시지까지 포함했습니다. 문서 진단도 확인했고 linter 오류는 없습니다.
```

### Turn 5 - User Prompt

```text
[p] 너는 C++ 테스트 전문가야.
[c] @docs/Regacy_Code_분석_리포트.md @README.md 를 참고하여 리팩토링의 각 단계별로 진행할 테스트 계획 문서를 만들어줘.
[t] 테스트 계획 문서는 아래를 포함해.
  - 현재 코드의 정상 동작을 테스트할 GoldenMaster Test
  - 현재 코드에서 버그로 인한 오동작이 예상되는 RED CASE
  - 리팩토링 각 1 커밋 단계별로 필요한 경계조건, Bug가 해소됨으로써 Green이 될 것으로 예상되는 리팩토링 테스트
[F] 기존 코드는 변경금지. DOCs 폴더에 Regacy_Code_리팩토링_테스트_계획.md 파일로 저장해줘.
```

### Turn 5 - AI Answer

```text
작성 완료했습니다.

`docs/Regacy_Code_리팩토링_테스트_계획.md`에 테스트 계획 문서를 추가했습니다. 포함 내용은 Golden Master Test, 현재 오동작 예상 RED CASE, 리팩토링 1커밋 단계별 Green 전환 테스트와 경계조건입니다.

기존 코드는 변경하지 않았고, 문서 진단 결과 linter 오류도 없습니다.
```

### Turn 6 - User Prompt

```text
e:\DEV\feedback_analyzer_13용 .cursorrules YAML 뼈대.
최상위 키만, 값은 비움:
project, code_style, architecture, tdd_rules, testing, forbidden, file_structure, ai_behavior
각 키 앞에 80자 폭 # 구분선 주석. YAML만 출력.
```

### Turn 6 - AI Answer

```yaml
################################################################################
project:

################################################################################
code_style:

################################################################################
architecture:

################################################################################
tdd_rules:

################################################################################
testing:

################################################################################
forbidden:

################################################################################
file_structure:

################################################################################
ai_behavior:
```

### Turn 7 - User Prompt

```text
tdd_rules 블록만 채워라. C++ UnitConverter 기준:
red_phase / green_phase / refactor_phase 각각에
description, rules(목록), must_not(목록). YAML만 출력.
```

### Turn 7 - AI Answer

```yaml
tdd_rules:
  red_phase:
    description: "UnitConverter의 기대 동작을 먼저 테스트로 정의하고, 실패하는 테스트를 확인한다."
    rules:
      - "변환 대상 단위와 기대 결과를 명확히 작성한다."
      - "정상 케이스보다 경계값과 오류 케이스를 우선 작성한다."
      - "테스트 이름은 입력, 동작, 기대 결과가 드러나게 작성한다."
      - "새 기능 구현 전에 반드시 실패하는 테스트를 먼저 추가한다."
    must_not:
      - "테스트 없이 UnitConverter 구현을 먼저 수정하지 않는다."
      - "실패 원인이 불명확한 테스트를 추가하지 않는다."
      - "여러 동작을 하나의 테스트에 과도하게 묶지 않는다."
  green_phase:
    description: "실패한 테스트를 통과시키기 위한 최소한의 UnitConverter 구현만 작성한다."
    rules:
      - "현재 RED 테스트를 통과시키는 가장 작은 변경만 수행한다."
      - "불필요한 일반화나 확장 설계를 하지 않는다."
      - "기존 Golden Master 동작이 깨지지 않는지 확인한다."
      - "정상 케이스와 오류 케이스가 모두 기대 결과를 만족해야 한다."
    must_not:
      - "아직 테스트가 없는 단위 변환 기능을 미리 구현하지 않는다."
      - "테스트를 통과시키기 위해 기대값을 임의로 낮추지 않는다."
      - "실패 테스트를 삭제하거나 비활성화하지 않는다."
  refactor_phase:
    description: "테스트가 모두 통과하는 상태에서 UnitConverter의 중복, 명명, 구조를 개선한다."
    rules:
      - "리팩토링 전후 모든 테스트가 동일하게 통과해야 한다."
      - "중복된 변환 로직과 매직 넘버를 의미 있는 구조로 정리한다."
      - "단위 이름, 변환 계수, 오류 처리 책임을 명확히 한다."
      - "동작 변경이 필요한 경우 새 RED 테스트를 먼저 작성한다."
    must_not:
      - "테스트 실패 상태에서 리팩토링을 진행하지 않는다."
      - "리팩토링 커밋에 새로운 기능 구현을 섞지 않는다."
      - "테스트 기대 결과를 구현에 맞춰 임의로 변경하지 않는다."
```

### Turn 8 - User Prompt

```text
🔲 검토
완성한 .cursorrules 전체를 검토하라. 수정 제안 금지.
보고만: YAML 문법 오류 / 누락 섹션 / tdd_rules vs forbidden 충돌 / Cursor가 지키기 어려운 규칙

4단계 — 전체 채우기
🔲 완성 YAML
C++ C++17 이상. code_style: clang-format, 네이밍·헤더/소스 분리 원칙
architecture: boundary/control/entity 정의 + 의존성 방향
testing: Catch2 고정, AAA, fixture 규칙, 커버리지 기준
forbidden: std::cout 디버그 남발 / 매직 넘버 하드코딩 / catch(...) 무분별 삼키기 / 헤더 내 구현 포함 / 전역 상태 남용
  (각각 pattern / reason / alternative)
file_structure: boundary/ control/ entity/ tests/ 트리 주석
ai_behavior: 레이어 위반 금지, 테스트 먼저, 리팩터는 green 이후
설명 없이 YAML 전체만.
```

### Turn 8 - AI Answer

```text
완성된 `.cursorrules` YAML 전체를 출력했습니다. 주요 구성은 project, code_style, architecture, tdd_rules, testing, forbidden, file_structure, ai_behavior입니다.
```

### Turn 9 - User Prompt

```text
작성된 커서룰을 .cursorrules에 저장해줘
```

### Turn 9 - AI Answer

```text
`.cursorrules`에 저장 완료했습니다.

파일 경로: `e:\dev\feedback_analyzer_13\.cursorrules`

저장 후 진단 확인 결과 linter 오류는 없습니다.
```

### Turn 10 - User Prompt

```text
보고서작성 및 백업
```

### Turn 10 - AI Answer

```text
보고서 작성 및 백업 범위 확인 질문을 표시했으나 사용자가 건너뛰었습니다.
```

### Turn 11 - User Prompt

```text
.cursor\agents에 있는 Agent 불러와
```

### Turn 11 - AI Answer

```text
`.cursor\agents`에서 `report-backup-agent.md`를 불러왔습니다.

이 Agent는 `보고서작성 및 백업` 명령을 받으면 다음 순서로 동작하도록 정의되어 있습니다.

1. Git 상태 확인
2. `report` / `prompting` 폴더 준비
3. 작업 보고서 작성
4. 프롬프트 기록 백업
5. 변경사항 커밋
6. GitHub 원격 저장소 push

안전 규칙도 포함되어 있으며, destructive git 명령과 민감정보 커밋은 금지되어 있습니다.
```

### Turn 12 - User Prompt

```text
보고서작성 및 백업
```

### Turn 12 - AI Answer

```text
report-backup-agent 규칙에 따라 보고서 작성, 프롬프트 기록 백업, Git 커밋과 원격 업로드를 진행합니다.
```

## 실행 명령 및 도구 출력

### Git 상태 확인

```text
## SPEC
 M README.md
?? .cursor/
?? .cursorrules
?? "Cursoe AI ... - ....md"
?? docs/
```

### 최근 커밋 확인

```text
1cf31c5 최초 커밋
```

### 기존 보고서 확인

```text
report/*: 0 files found
prompting/*: 0 files found
```

### 문서 진단

```text
README.md: No linter errors found.
.cursorrules: No linter errors found.
docs/Regacy_Code_분석_리포트.md: No linter errors found.
docs/Regacy_Code_리팩토링_테스트_계획.md: No linter errors found.
```

## 최종 상태

커밋과 push 결과는 작업 완료 후 보고서에 반영한다.
